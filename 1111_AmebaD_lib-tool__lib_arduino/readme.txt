1, add all application build ".o" files into "o_lib" folder from GCC project

2, start CMD

3, at CMD enter "make arduino" (this tool will auto delete the main.o and combine all other .o files together into lib_arduino.a)

4, check the "lib_arduino.a" "Date modified"

5, copy and paste the "lib_arduino.a" into Arduino package